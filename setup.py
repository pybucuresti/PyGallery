from setuptools import setup, find_packages

setup(name='PyGallery',
      version='0.1dev',
      packages=find_packages(),
      include_package_data=True,
      zip_safe=False,
      install_requires=[
      ],
)
